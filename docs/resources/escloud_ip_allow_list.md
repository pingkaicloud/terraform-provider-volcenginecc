---
page_title: "volcenginecc_escloud_ip_allow_list Resource - terraform-provider-volcenginecc"
subcategory: "ESCloud"
description: |-
  IP allowlist information
---

# volcenginecc_escloud_ip_allow_list (Resource)

IP allowlist information

## Example Usage

```terraform
resource "volcenginecc_escloud_ip_allow_list" "Example" {
  instance_id = "o-008wv7krnmw4"
  type        = "PRIVATE_ES"
  allow_list  = ["10.0.0.0/16", "192.168.1.1"]
  groups = [
    {
      name       = "group1"
      allow_list = ["10.0.0.0/16", "192.168.1.2"]
    },
    {
      name       = "group2"
      allow_list = ["192.168.2.1", "127.0.0.1"]
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `allow_list` (Set of String) Default group IP allowlist list; separate multiple IPs with commas (,)
- `groups` (Attributes Set) Custom group IP allowlist
 Important Note: When using SetNestedAttribute, you must fully define all attributes of its nested structure. Incomplete definitions may cause Terraform to detect unexpected differences during plan comparison, triggering unnecessary resource updates and affecting resource stability and predictability. (see [below for nested schema](#nestedatt--groups))
- `instance_id` (String) Instance ID
- `type` (String) Network type
  
    - PRIVATE_ES: Instance private network access
    - PRIVATE_KIBANA: Kibana private network access
    - PUBLIC_ES: Instance public network access
    - PUBLIC_KIBANA: Kibana public network access

### Read-Only

- `id` (String) Uniquely identifies the resource.

<a id="nestedatt--groups"></a>
### Nested Schema for `groups`

Optional:

- `allow_list` (Set of String) Custom group IP allowlist list cannot be empty; to configure multiple IPs, use commas (,)
- `name` (String) Custom group name

## Import

Import is supported using the following syntax:

```shell
$ terraform import volcenginecc_escloud_ip_allow_list.example "instance_id|type"
```
